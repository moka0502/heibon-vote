# UI/CX/ネイティブ感 改善バックログ

作成日: 2026-08-15。Duolingo/Typeform/Slido等の「何がイケているか」を要素分解し、現行UIとのギャップを洗い出したもの。追加で「イケてるアプリとの比較」でも探索した。優先度をつけるなら、効果高×難易度低のものから着手するのが費用対効果が良い。

新しい改善点に気づいたら随時ここに追記する。実装したら「実装済み」に移す。

## 実装済み(2026-08-15)

- 即時フィードバックを`.match`/`.mismatch`のテキスト色だけから、○✕アイコン+背景色の`.feedback-banner`に強化(`renderQuestionFeedback`)
- %バーを`width:0%`→目標値へアニメーションさせる`animateBarWidth`ヘルパーを新設、`renderQuestionFeedback`・`renderTopicBreakdown`両方に適用(Slido的な「結果が伸びてくる」演出)
- クイズ画面に進捗バー(`.quiz-progress-track`)を追加
- 選択肢タップ時、選んだボタンを180ms間ハイライトしてから次へ進むよう変更(タップした実感を持たせる)
- `prefers-reduced-motion`対応

**追加実装(2026-08-15、効果高×難易度低の6件)**:
- UI#1: `renderVoteBreakdownRow`(結果画面・履歴詳細で共有)を素のテキストから%バー表示に変更。`GET /api/sessions/:id`が`options`・`percentages`を返すようサーバー側(`sessions.js`)も拡張
- CX#1・CX#8: 初回起動時、属性入力の前に`renderIntro`(アプリの目的・「平凡」の意味付け・属性を聞く理由の説明)を挟むように`init()`を変更
- CX#6・Native#11: 結果画面に「結果をシェアする」ボタンを追加。`navigator.share`があればOS標準の共有シート、なければクリップボードコピー+「コピーしました!」表示にフォールバック
- CX#10: 結果画面に「もう一度挑戦する」ボタンを追加、ホーム経由不要でランダム再挑戦できるように

**追加実装(2026-08-15夜、効果中〜高×難易度低の12件)**:
- UI#5・UI#6・UI#11・M1: フォーカスリングをブランドカラーに統一、`.btn-link`のタップ領域拡大(44px目安)、スコア・%表示に`tabular-nums`指定、`.card`に軽いbox-shadowを追加
- UI#2・UI#16・CX#14: `renderError`の生の技術メッセージを常に同じやさしい固定文言に統一(詳細はconsoleへ)。お題投稿フォームの空文字送信にインライン警告を追加
- CX#16: 属性別内訳の閾値未達メッセージに進捗バーを追加
- UI#4・UI#7・UI#12・R3: Homeの「履歴を見る」だけ太字で軽く強調、カテゴリ選択ボタンに`.btn-outline`を追加してクイズ回答ボタンと区別、ページ見出しとクイズ質問文(`.question-heading`)でh2の重み付けを分離、内訳バーで選ばなかった選択肢を`.bar-muted`で落ち着かせ「あなたの一票」が一目で分かるように

**追加実装(2026-08-15深夜、スプリント4〜6の10件)**:
- スプリント4 (Native#4・UI#9・UI#13・UI#8・CX#7・H1): `viewport-fit=cover`+`env(safe-area-inset-*)`でノッチ・ホームインジケータ対応。結果画面・履歴詳細のスコアカードに`.card-hero`で余白の強弱。非選択ボタンに`scale(0.98)`。履歴が空のとき・属性別内訳のデータ不足時の文言を「平凡」の世界観に寄せてやさしく
- スプリント5 (UI#3): `renderLoading()`(スピナー)+`showLoading()`ヘルパーを新設。画面遷移中(API待ち)にローディング表示。history操作を経由しないため戻るボタンの履歴管理には影響しない
- スプリント6 (S1・S3・R1): `POST /api/votes`が`totalVotes`を返すようになり、クイズの即時フィードバック画面に「n件の回答から算出」を表示。自分が選んだ選択肢の行だけ着地の瞬間に一度だけハイライトする`pulse-you`アニメーション。`animateCountUp()`を新設し%・件数ラベルをバーの伸びと歩調を合わせてカウントアップ

未実装: 画面遷移そのもの(Typeform的なスライド)。マスコットは今回スコープ外の方針。

## UI

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| ~~1~~ | ~~結果画面(`renderResult`)・履歴詳細(`renderHistoryDetail`)の内訳がバーなしの素のテキスト羅列で、即時フィードバック画面と表現が不一致~~ → 実装済み(2026-08-15) | 高 | 低 |
| ~~2~~ | ~~`renderError`が`err.message`を生の技術メッセージのまま表示(例: "request failed: 500")~~ → 固定のやさしい文言に統一して実装済み(2026-08-15) | 中 | 低 |
| ~~3~~ | ~~画面遷移中(API待ち)にローディング表示がなく、通信が遅いと固まって見える~~ → `renderLoading()`+`showLoading()`で実装済み(2026-08-15) | 高 | 中 |
| ~~4~~ | ~~Homeの副次ボタン(履歴/お題内訳/設定/提案)が全部同じ下線テキストリンクで優先順位が伝わらない~~ → 「履歴を見る」を太字化して実装済み(2026-08-15) | 中 | 低 |
| ~~5~~ | ~~フォーカスリングがブラウザデフォルトのまま、ブランドカラーと未統一~~ → 実装済み(2026-08-15) | 低 | 低 |
| ~~6~~ | ~~`.btn-link`のタップ領域が上下8pxのみで、モバイル推奨サイズ(44px)を下回る可能性~~ → 実装済み(2026-08-15) | 低 | 低 |
| ~~7~~ | ~~カテゴリ選択ボタンとクイズ回答ボタンが同じ`.btn`見た目で、意味の違う操作が視覚的に区別されない~~ → `.btn-outline`追加で実装済み(2026-08-15) | 中 | 低 |
| ~~8~~ | ~~空状態(履歴なし・データ不足)のメッセージが事務的で素っ気ない~~ → 履歴・内訳の空状態文言をやさしく変更して実装済み(2026-08-15) | 低 | 低 |
| ~~9~~ | ~~カード間の余白が全画面一律で、情報の重要度に応じた強弱がない~~ → 結果・履歴詳細のスコアカードに`.card-hero`を追加して実装済み(2026-08-15) | 中 | 低 |
| 10 | ボタンが primary/link の2択のみで、中間ランクの表現力がない(`.btn-outline`はカテゴリ選択専用として追加済みだが汎用の中間ランクとしては未整理) | 中 | 低 |
| ~~11~~ | ~~スコアや%の数字にtabular-numsが未指定で、アニメーション中に字幅がガタつく~~ → 実装済み(2026-08-15) | 低 | 低 |
| ~~12~~ | ~~見出し(h2)が全画面同サイズで、ページ見出しと質問文の重み付けが視覚的に伝わらない~~ → `.question-heading`追加で実装済み(2026-08-15) | 中 | 低 |
| ~~13~~ | ~~選択後、非選択ボタンがただ薄暗くなるだけで情報として無機質~~ → `scale(0.98)`を追加して実装済み(2026-08-15) | 低 | 低 |
| 14 | ダークモード非対応(`prefers-color-scheme`のハンドリングなし) | 中 | 中 |
| 15 | 称号バッジが単色ピル固定で、ランクが上がっても見た目に差がない | 中 | 中 |
| ~~16~~ | ~~フォーム送信のバリデーション失敗が無反応(空文字送信を静かに握りつぶす)~~ → お題投稿フォームにインライン警告を追加して実装済み(2026-08-15) | 中 | 低 |
| 17 | 画面遷移時のスクロール位置管理が未検証 | 低 | 低 |
| 18 | アイコンが一切なくテキストのみで、画面のパッと見識別がしづらい | 高 | 中 |

## CX

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| ~~1~~ | ~~初回、属性入力フォームがいきなり出てくる~~ → `renderIntro`追加で実装済み(2026-08-15) | 高 | 低 |
| 2 | 結果への感情的フィードバックが薄い。「なぜこの結果か」「次どうすればいいか」の文脈がない。~~称号システム(`tiers.js`)も閾値未調整~~ → 閾値はモンテカルロシミュレーションで検証・調整済み(2026-08-15、下記参照)。文脈づけのコピーは未着手のまま | 中 | 中 |
| 3 | リピートを促す設計がゼロ(新しいお題が増えたことを伝える手段がない) | 中 | 中 |
| 4 | 属性別内訳を見る「発見の瞬間」を演出する導線がなく、見に行くモチベーションが弱い | 中 | 低 |
| 5 | お題を提案しても採用されたか分からず、行動への応答がない(手動審査のため) | 中 | 中 |
| ~~6~~ | ~~シェア機能がゼロ~~ → 結果画面に「結果をシェアする」ボタン追加で実装済み(2026-08-15) | 高 | 低 |
| 7 | 「平凡」という世界観がUIコピーに滲み出ておらず、業務アプリ的な文言のまま。空状態・エラー文言は着手済み(2026-08-15)だが、全画面の網羅的な見直しはまだ残る | 中 | 低 |
| ~~8~~ | ~~「平凡である/ない」ことの意味付け・面白がり方がコピーで提示されていない~~ → `renderIntro`で実装済み(2026-08-15) | 高 | 低 |
| 9 | 属性を後から変更した場合、過去データへの影響が説明されない | 低 | 低 |
| ~~10~~ | ~~結果画面から即もう一度遊べず、ホーム経由で2手かかる~~ → 「もう一度挑戦する」ボタン追加で実装済み(2026-08-15) | 高 | 低 |
| 11 | 「お題の内訳を見る」に入る前に何が得られるか予告がない | 中 | 低 |
| 12 | 「あなたの傾向」のようなパーソナライズされた振り返りコンテンツがない | 中 | 中 |
| 13 | お題投稿へのインセンティブがゼロ(採用通知・掲載等なし) | 中 | 中 |
| ~~14~~ | ~~エラー時、自分のせいかアプリのせいか分からず不安にさせる文言~~ → `renderError`の文言統一で実装済み(2026-08-15) | 低 | 低 |
| 15 | 連続プレイ日数(ストリーク)のような継続動機の数字がない | 高 | 中 |
| ~~16~~ | ~~100件閾値未達時の「内訳が見れない」体験がそのままガッカリ体験になる~~ → 進捗バーを追加して実装済み(2026-08-15) | 中 | 低 |
| 17 | カテゴリを選んで遊んだ後、そのカテゴリならではの回収(まとめ)がない | 中 | 中 |

## ネイティブ感

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| ~~1~~ | ~~ブラウザの戻る/スワイプバックと連動していない~~ → `mount(node, backTo)`+History API(pushState/popstate)連動で実装済み(2026-08-15) | 高 | 中 |
| 2 | 固定ナビゲーション(タブバー)がなく、毎回Homeに戻る「Webサイト的」な階層移動 | 中 | 中 |
| 3 | 画面遷移が瞬間切り替え(方向性のあるトランジションがない) | 中 | 中 |
| ~~4~~ | ~~`<meta viewport>`に`viewport-fit=cover`がなく、iPhoneのノッチ・ホームインジケータ領域にコンテンツが被る可能性~~ → `viewport-fit=cover`+`env(safe-area-inset-*)`で実装済み(2026-08-15、実機(ノッチ付き端末)では未検証) | 中 | 低 |
| 5 | オフライン時の専用表示がない(Service Workerは静的シェルのみ対応、API失敗時は通常のエラー画面) | 中 | 中 |
| 6 | プルトゥリフレッシュがない | 低 | 中 |
| 7 | スクロールの慣性/バウンス挙動(`overscroll-behavior`)が未確認 | 低 | 低 |
| 8 | ステータスバー色が画面ごとに連動していない | 低 | 中 |
| 9 | 起動時のブランドスプラッシュがなく、一瞬白画面が出うる | 中 | 低 |
| 10 | アプリアイコンのバッジ機能がない(新着お題等の未読表現、PWAの制約もある) | 低 | 高 |
| ~~11~~ | ~~OS標準の共有シートを呼ぶ動線がない~~ → `navigator.share`対応(未対応環境はクリップボードコピーへフォールバック)で実装済み(2026-08-15) | 高 | 低 |
| 12 | 長押し(コンテキストメニュー)操作が一切ない | 低 | 中 |
| 13 | フォーム入力のキーボード最適化(自動大文字化オフ等)が未検証 | 低 | 低 |
| 14 | 横向き・iPad幅など画面回転/広い画面での見え方が未検証 | 低 | 中 |
| 15 | 結果画面でのジェスチャー的インタラクション(シェイク等)がない | 低 | 高 |
| ~~16~~ | ~~バックグラウンド復帰時、クイズ途中状態が保持されるか未確認~~ → `localStorage`にクイズ進行状態を保存し`init()`で再開する実装済み(2026-08-15、堅牢性タスクと表裏) | 高 | 中 |
| 17 | アプリ内通知(トースト/バナー)の仕組みが存在しない | 中 | 中 |

## イケてるアプリとの比較で見つけたもの

| 参考 | 発想 | 改善点 | 効果 | 難易度 |
|---|---|---|---|---|
| Instagram Stories / TikTok | 全画面・連続スワイプ体験 | 今はカード内固定のクイズ画面。お題を全画面フルスクリーン+スワイプで進める体験にすると没入感が上がる | 中 | 高 |
| Headspace等の落ち着き系アプリ | 余白と呼吸感 | 今のUIは全体的に詰まっている。行間・余白を意図的に広げて「サクサクだけど急かされない」感じを出せる | 中 | 低 |
| Robinhood/ゲームアプリの達成演出 | レア達成の紙吹雪演出 | 満点などのレアケースだけ、通常のGSAP演出と別次元のお祝い演出(紙吹雪・全画面カラー)を入れると記憶に残る | 中 | 中 |
| Spotify Wrapped | 期間まとめのシェアコンテンツ | 「あなたの平凡度、今月のまとめ」のような、シェアしたくなる縦長画像を自動生成する機能 | 高 | 高 |
| Google系アプリのMaterial You | 選択肢に応じた動的配色 | カテゴリごとにアクセントカラーを少し変える(食事は暖色寄り、お金は落ち着いた色等)と、カテゴリ選択の体験に厚みが出る | 低 | 中 |

## 元祖3本(Duolingo/Typeform/Slido)を掘り下げて追加分(2026-08-15)

### Duolingo

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| D1 | 正誤フィードバックがサウンドを伴わず視覚のみ。Duolingoは正解/不正解に効果音がある | 中 | 低 |
| D2 | ストリーク(連続日数)とは別に、1回のプレイ内での「今日の小さな目標」提示がない | 低 | 中 |
| D3 | プロフィール設定がネイティブの`<select>`依存。Duolingoのような大きくタップできる選択肢ボタンになっておらず、オンボーディングが事務的に感じる | 中 | 低 |

### Typeform

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| T1 | 前の質問に戻って回答を修正できない一方通行設計。Typeformは前の質問に戻って編集できる | 中 | 中 |
| T2 | 質問文のタイポグラフィが固定h2サイズのまま。Typeformは「回答者の意識を1問に集中させる」ための大きさ・余白がある | 中 | 低 |
| T3 | 完了後のサンクス画面(お題投稿完了等)が汎用的。Typeformは回答内容に応じて動的に変わるサンクスコンテンツを出す | 低 | 中 |

### Slido / Mentimeter

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| ~~S1~~ | ~~母数(何人が回答したか)が画面に一切出ない。%だけでは規模感・信頼度が伝わらない~~ → クイズ即時フィードバック画面に「n件の回答から算出」を追加して実装済み(2026-08-15、結果/履歴の内訳一覧は10行分の繰り返しになり煩雑なため対象外) | 中 | 低 |
| S2 | 可視化が常に横棒グラフ一種類のみ。Slidoは棒グラフ以外の表現も使い分ける | 低 | 高 |
| ~~S3~~ | ~~自分の一票が「たった今反映された」という参加実感を強調する演出がない~~ → 自分の選択肢の行だけ一度ハイライトする`pulse-you`アニメーションで実装済み(2026-08-15) | 中 | 低 |

## 新規2本(Wordle/NYT Games, Tinder/Bumble)からの追加分(2026-08-15)

### Wordle / NYT Games

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| W1 | 1日1回だけという儀式性がない。何度でも遊べる分、「今日の分」という特別感がゼロ | 高 | 中 |
| W2 | ネタバレなしでシェアできる仕組みがない(絵文字グリッド的な、結果の"形"だけ見せる工夫) | 高 | 中 |
| W3 | 全員が同じ問題を解く日がなく、「今日はどうだった？」を友人と語り合う共通の土台がない | 高 | 中 |

### Tinder / Bumble

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| B1 | 次の問題が後ろに透けて見えるような「カードの厚み」表現がなく、残り枚数が物理的に感じられない | 低 | 中 |
| B2 | スワイプで回答できず、タップのみ。2択だからこそ左右スワイプに向いている操作性 | 中 | 中 |
| B3 | 演出強度が常に一定。「マッチ成立」のような特別な瞬間だけ強い演出をする非対称性がない | 中 | 低 |

## 既出5本(Instagram Stories/TikTok, Headspace系, Robinhood, Spotify Wrapped, Material You)を深掘りした追加分(2026-08-15)

### Instagram Stories / TikTok

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| IG1 | 進捗バーが1本の連続バーで、Stories式の「問題ごとに区切られたセグメント」表示になっていない | 低 | 低 |
| IG2 | 画面タップ位置(右=次へ、左=前へ)で操作できる直感的操作がない | 低 | 中 |
| IG3 | 常に能動的なタップ待ちで、結果表示などを自動で進める「ながら見」的な受動体験がない | 低 | 中 |

### Headspace系(落ち着き系アプリ)

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| H1 | コピーが「エラーが発生しました」等、終始機能的で無機質。Headspaceは常にやさしい語りかけをする。エラー文言・空状態文言は着手済み(2026-08-15)だが、全画面の網羅的な見直しはまだ残る(CX#7と表裏) | 中 | 低 |
| H2 | ボタンを押すと即座にクイズが始まり、心の準備をするワンクッションがない | 低 | 低 |
| H3 | 背景が完全に静止した単色。微細な揺らぎ等「生きている」感じがない | 低 | 中 |

### Robinhood/ゲームアプリ

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| ~~R1~~ | ~~%バーの幅はアニメーションするが、隣の数字ラベル(79%等)自体は瞬間表示でアニメーションしない~~ → `animateCountUp()`で実装済み(2026-08-15) | 低 | 低 |
| R2 | 称号システムはあるが、「次のランクまであと◯問」のような目標との距離感を見せる演出がない | 中 | 中 |
| ~~R3~~ | ~~多数派/少数派を示すバーに一貫した意味づけ色がない(常にインディゴ1色)~~ → 「あなたが選んだ選択肢」以外を`.bar-muted`で落ち着かせる形で実装済み(2026-08-15、多数派/少数派の色分けではなく「あなたの一票」の可視化として実装) | 中 | 低 |

### Spotify Wrapped

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| SW1 | 「他のプレイヤーと比べてどうか」という相対順位・パーセンタイル演出がない | 高 | 中 |
| SW2 | 結果コピーが淡々とした事実提示のみ。「実はあなたは○○な人でした」のような意外性の演出がない | 中 | 低 |
| SW3 | 結果画面固有のビジュアルアイデンティティがなく、他画面と同じ`.card`の延長線上 | 中 | 中 |

### Google Material You

| # | 改善点 | 効果 | 難易度 |
|---|---|---|---|
| ~~M1~~ | ~~`.card`がborder+背景色のみでshadowがなく、階層の奥行き感がゼロ~~ → 軽いbox-shadowを追加して実装済み(2026-08-15) | 低 | 低 |
| M2 | タップ時、位置に応じて波紋が広がるリップルエフェクトがない(scale縮小のみ) | 低 | 中 |
| M3 | 画面遷移時、共通要素(選んだボタン等)が視覚的に連続する「shared element」的な演出がない | 中 | 高 |
