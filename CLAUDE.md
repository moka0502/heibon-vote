# heibon-vote プロジェクト固有メモ

「真の平凡投票アプリ」。ランダムに10問出題し、各問で「世間の多数派」と何問一致できるかを競うクイズアプリ。属性(年代・性別)は各投票にタグ付けして将来の内訳表示に備えるが、多数派判定・平凡度スコア自体には使わない。将来的にiOS化(まずPWA、必要ならApp Store配信、kusutto-games同様Capacitorでラップする方針)を検討する。

共通の開発標準は `~/.claude/CLAUDE.md`(postCreateCommandで自動配置)を参照。ここにはこのプロジェクト固有の制約・quirksのみ書く。現行動作の正確な記述は`SPEC.md`を参照(データモデル・API・多数派判定ロジック・平凡度ランクの閾値など)。

## 実機確認済み(2026-08-15、devcontainer上でchromium+playwright-coreにより検証)

前回セッション(Windowsホスト直下、`better-sqlite3`のネイティブ差異で未確認だった9項目チェックリスト)をdevcontainer上で実際にブラウザ操作して全項目通過を確認した。過程で見つかった3件のバグは修正・再検証済み:

1. `.btn-link`ボタン(「履歴を見る」等)が連続すると`display:inline-block`で隙間なくくっつき、Home画面・結果画面で読めない一続きのリンクに見えていた → `www/css/style.css`の`.btn-link`を`display:block`に変更
2. `.bar-option-label`が`width:6.5em`固定+`text-overflow:ellipsis`で、内訳バーの「(あなた)」「(多数派)」タグが省略されて見えなくなっていた(今回の目玉機能が実質死んでいた) → `width`→`min-width`、`overflow/text-overflow/white-space`指定を削除
3. クイズ中にブラウザ「戻る」でHomeへ離脱しても`localStorage`のクイズ進行状態がクリアされず、直後にリロードすると中断したクイズへ強制的に戻されていた(「戻るボタン連動」と「リロード時再開」機能が組み合わさって起きた副作用) → `www/js/app.js`の`showHome()`冒頭で`clearQuizState()`を呼ぶように変更

以後、新たな大きめの変更をした際は同様にdevcontainer上での実機確認(可能ならplaywright-core+システムchromiumでのヘッドレス操作)をコミット前に行うこと。

## 実機フィードバック対応(2026-08-16、Tailscale経由でiPhoneから実プレイ)

スマホからのアクセスがTailscale(userspace-networkingモード+`tailscale serve`)で確立できたので、初めて実機で通しプレイし、11件のフィードバックのうち方針が決まった6件を対応した。

- **過去の大規模自動テストが実データを汚染していた発覚**: 「111件の回答」の内訳を調べたところ、初期データ100件+`is_dummy=0`(実データ扱い)の11件で、その11件の`voter_id`が`roundcheck-0.28...`等の明らかな自動テスト由来だった。過去の大規模テストが本番用の`votes`/`quiz_sessions`テーブルに実データとして投票・履歴を投入したまま放置されていたことが判明。投票407件・セッション24件を削除しゼロから開始する状態に戻した。**教訓**: 大規模テストでvote/session APIを叩く際は、`is_dummy=1`にするか、テスト後に確実にクリーンアップする運用に倒す必要がある(今回は未実施のまま気づかれずに残っていた)
- 「もう一度挑戦する」→ランダム再挑戦ではなくカテゴリ選択画面に戻すよう変更(`onRetry: () => showCategoryPicker()`)
- **履歴は個人別ではなく全員分が見えていた**バグを修正。`quiz_sessions`に`voter_id`列を追加(`server/db.js`の`migrate()`に追随)、`GET/POST /api/sessions`・`GET /api/sessions/stats`を`voterId`で絞り込むように変更。「これまでの挑戦者◯人中」のパーセンタイルだけは全員分のまま(他者との比較なので個人化しない)
- 「お題の内訳」一覧にカテゴリ見出しを追加(`GET /api/topics`が`category`/`categoryLabel`を返すよう変更、`c.sort_order`→`question`順)
- 回答バーの長さが揃わない不具合を修正。`.bar-option-label`が`min-width`だったため「(あなた・多数派)」タグ付きの行だけラベル欄が伸び、隣の`.bar-track`(`flex:1`)の幅が行ごとにばらついていた → `width`固定に変更(2026-08-15にタグが見えなくなるバグを直した際と同じ`overflow`/`ellipsis`は付けない)
- ボタンのテキスト中央揃えルールを統一: 単独の行動ボタン(`.btn-primary`/`.btn-outline`)は中央揃え、複数から選ぶ一覧ボタン(クイズ回答肢・属性設定の値・カテゴリ選択)は左揃えのまま。`.attribute-option`/`.category-option`クラスで左揃えに上書き
- 年代の選択肢に「0歳代」区分を新設するのではなく、一番若い区分(`10s`)のラベルを「10代」→「19歳以下」に変更。`server/seed.js`の`attribute_values`が`INSERT OR IGNORE`だったため、`categories`/`topics`と同じUPSERTパターンに直し、JSON編集がDBに反映されるようにした(このバグがある間は同種のラベル変更が今後も反映されなかった)

保留にした項目(次回以降):
- タップ後の自動送り(180ms)vs「次へ」ボタン: ユーザーが現状維持で合意済み、対応不要
- ~~平凡度ランクの言い回し・パーセンタイル文言・平凡メーター~~ → 2026-08-16に対応済み(下記「言い回し刷新」参照)

## 言い回し刷新 + カテゴリのPart分割 + 対象限定問題の削除(2026-08-16)

上記の保留事項をユーザーと相談しながら実装した。

- **段階ラベルの言い換え**: 「平凡=つまらない」ではなく「平凡=共感力があって王道」というアプリの主張(イントロ画面の「『平凡』は、実はすごい」)に一貫させる形で、`server/tiers.js`の`sessionTierFor`が返す文字列を変更(閾値の数字は変更なし)。`かなり平凡`→`平凡寄り`、`平凡`→`個性あり`。`真の平凡(今回)`/`個性派`/`唯一無二`は変更なし
- **平凡メーター**: 結果画面に`.tier-meter`(横帯+連続的に動く矢印)を追加。段階ラベルだけだと8/10と9/10が同じ枠になる粗さを、実スコアに応じた矢印位置で補う
- **パーセンタイル文言の言い換え+バグ修正**: 「あなたと同じかそれ以上『平凡』だったのは◯%」という文言が、実は`WHERE match_count <= ?`(=自分と同じか自分より少ない人)を数えており**クエリの向きと文言の向きが逆だった**ことが判明。「あなたより『共感性が高く、定番を理解し、万人受けする王道』だった人は◯人でした」という人数ベースの文言に変更し、同時に正しい向き(`match_count > ?`)に修正した(`server/routes/sessions.js`、レスポンスの`percentile`→`moreCommonCount`)
- **カテゴリをPart1/Part2で選べるように**: カテゴリボタンの「(11問)」のような半端な数の表示が気持ち悪いという指摘を受け、常に10問ぴったりの束で選べる構造に変更。Part1はカテゴリ内で常に同じ固定10問(`topics`の`rowid`順)、Part2はPart1を除いた残り全部からランダムに10問(残りが10問未満のカテゴリはPart2ボタンを出さない)。`GET /api/topics/random`に`part`パラメータを追加(`server/routes/topics.js`)
- **「夫のお小遣い」問題を削除**: 対象が限定される(誰でも回答できるとは限らない)問題は削除する方針を再確認し監査。該当は`okozukai-sei`の1問のみで、`questions-seed.json`・DB(`votes`/`options`/`topics`)から削除した

**副作用の解消(2026-08-16)**: 上記の削除で「お金」カテゴリが9問に減りカテゴリ選択画面から消えていた件、`生命保険に入っている派? 入っていない派?`(id: `seimei-hoken-kanyu`)を実データ根拠(公益財団法人生命保険文化センター「2024年度 生命保険に関する全国実態調査」)で追加し10問に復旧・確認済み

## 現状(2026-08-15)

問題バンクは127問(うち`status:"active"`122問、`status:"stock"`5問)。**11カテゴリ全てがactive化され、カテゴリ選択・ランダムどちらにも登場する**(2026-08-15夜、残り6カテゴリを実データ調査で埋めて完了): 食事11/お金10/恋愛10/季節行事10/健康カラダ10/住まい身支度12/余暇娯楽12/仕事働き方12/デジタル習慣12/持ち物12/ジンクス運試し11。目標は各カテゴリ12問(以前は10問だったが引き上げ)だったが、ジンクス・運試しのみ信頼できる実データが10件しか見つからず11問で確定(コックリさん・流れ星の願い事・風水・茶柱等は出典なしで見送り)。「靴下(右足/左足)」の仮置き問題は出典が見つからず削除。全問実データ根拠・2択にタグ付け済み。仮置き(実データなし)の問題は全廃した。`server/data/questions-seed.json`への追記で継続する運用(追記だけで既存の実票・履歴を壊さず反映されることを確認済み)。残る5問のstockは既にactiveな5カテゴリ内の温存分(削除ではなく取捨選択の結果)。

カテゴリは当初「嗜好品・持ち物」「睡眠・生活リズム」の2つが作りづらく伸び悩んだため、健康寄りの問題(酒・タバコ・睡眠系)は「健康・カラダ」に統合し、代わりに「恋愛」「健康・カラダ」「仕事・働き方」「デジタル習慣」「ジンクス・運試し」を新設(2026-08-15)。データが取りやすいテーマ(企業アンケート文化が厚い/恋愛婚活系の調査が多い)を優先する方針に転換した。

`status`は`active`(ランダム出題・お題一覧に出る)/`stock`(優先度を下げて温存中、データはあるが今は出題しない)の2値。カテゴリ内で取捨選択した際に「削除」ではなく`stock`に落とす運用。

母集団は`votes`テーブルに`is_dummy=1`で投入した初期データと、実際のプレイで貯まる実票を合算して多数派判定に使う(自己増殖的に育つ設計)。ただし**実データ(is_dummy=0、同一voter_idは最新1票のみ)が100件を超えたお題は、以後初期データを無視して実データのみを正とし、不要になった初期データはDELETEする**(`server/majority.js`)。属性別内訳(`/:id/breakdown`)も同じ実データ100件を閾値に、届いていなければ「まだ表示できません」に倒す。

追加で以下を実装、devcontainer上で実機確認済み:
- **属性別内訳表示**: `GET /api/topics`(active一覧)・`GET /api/topics/:id/breakdown`。属性は年代・性別に加え血液型・利き手も追加(`server/data/attributes-seed.json`)。血液型・利き手を「問題」ではなく「属性」にしたのは、選べない/変わらない情報は多数派当てゲームに向かず、属性別の傾向可視化(「A型はこう回答しがち」)にこそ向くと判断したため
- **属性設定画面**: Home→「あなたについての設定」で保存済み属性を編集可能(`renderProfileForm`の編集モード)
- **お題ごとの即時フィードバック**: 回答するとその場で平凡判定+選択肢ごとの%を表示してから次の問題へ(`renderQuestionFeedback`)。`POST /api/votes`のレスポンスに`percentages`を含める
- **お題投稿フォーム**: Home→「お題を提案する」→`POST /api/suggestions`(`suggestions`テーブル、審査は今のところ手動)
- **同一端末の複数回答は最新1票のみ集計**: `localStorage`に`voterId`(UUID)を持たせ、投票に添付。`votes.voter_id`列、集計は`server/votes-dedup.js`の`getLatestRealVoteRows`で常にvoter_idごとの最新行のみ使用
- **PWA化**: `www/manifest.webmanifest` + `www/sw.js`(静的シェルのみcache-first、`/api/*`は素通し)+ アイコン(`www/icons/`、chromiumでのスクリーンショット生成、インディゴ背景に「平」)
- **演出強化**: kusutto-games同様、GSAPをCDNではなくローカル同梱。結果画面のスコアをカウントアップ+ランクをポップインさせる演出のみ(`www/js/effects.js`)

次期ロードマップ(画面遷移図・UI参考アプリ・iOSネイティブ機能候補・セキュリティチェックリスト)はArtifactに整理済み: https://claude.ai/code/artifact/f06b23ca-950b-4854-8798-e36e4b4555b9

**UI洗練化(2026-08-15、マスコットなしの方針で着手)**: 実装済み分・未着手分(70件、効果/難易度つき)は`UX-BACKLOG.md`が一つの事実源。効果高×難易度低の6件+効果高×難易度中の3件(平凡度ランクの閾値調整、ブラウザ戻る/スワイプバック連動、バックグラウンド復帰時のクイズ状態保持)を同日中に実装済み。残りは中〜低効果のものが大半。新しい気づきはそこに追記していく運用。

**平凡度ランクの閾値調整(2026-08-15)**: 実プレイデータがまだない状態で「調整」するため、`questions-seed.json`のdummyVotes比率を使ったモンテカルロシミュレーション(母集団分布どおりに回答する「典型的な人」を2万セッション分試算)で検証。元の閾値だと最低ランク「唯一無二」の出現率が0.02%(実質到達不能)だったため、`server/tiers.js`の個性派/唯一無二境界を0.2→0.3に引き上げ、約0.4%(1/250程度)まで緩和した。「平凡」(0.5〜0.8)は64.8%とちょうど良い塊で妥当と判断、変更なし。

**ブラウザ戻る連動・クイズ状態の永続化(2026-08-15)**: `www/js/app.js`の`mount(node, backTo)`が`history.pushState`/`popstate`と連動し、ブラウザの戻る・スワイプバックが画面遷移として機能するように。`www/js/storage.js`に`saveQuizState`/`getQuizState`/`clearQuizState`を追加し、クイズ進行中は1問ごとに`localStorage`へ保存、`init()`起動時に未完了のクイズがあれば再開する。 Duolingo/Typeform/Slidoの「何がイケているか」を要素分解し、ギャップを埋めた。①即時フィードバックを`.match`/`.mismatch`のテキスト色だけから、○✕アイコン+背景色の`.feedback-banner`に強化(`renderQuestionFeedback`) ②%バーを`width:0%`→目標値へアニメーションさせる`animateBarWidth`ヘルパーを新設、`renderQuestionFeedback`・`renderTopicBreakdown`両方に適用(Slido的な「結果が伸びてくる」演出) ③クイズ画面に進捗バー(`.quiz-progress-track`)を追加、テキストの「3/10問目」だけだったのを視覚化 ④選択肢タップ時に即座に画面遷移せず、選んだボタンを180ms間ハイライトしてから次へ進むよう`renderQuizQuestion`を変更(タップした実感を持たせる)。`prefers-reduced-motion`にも対応済み。画面遷移そのもの(Typeform的なスライド)は未着手で残タスク

残タスク:
- ~~残り6カテゴリ(住まい身支度/余暇娯楽/仕事働き方/デジタル習慣/ジンクス運試し/持ち物)~~ → 2026-08-15夜、WebSearchで実データを収集し全カテゴリactive化して解消。ジンクス・運試しのみ12問目標に1問届かず11問(上記「現状」参照)
- ~~ランダム出題に加えて「カテゴリを選んで出題」モードの検討~~ → 2026-08-15実装済み。`categories`テーブル新設(`server/data/categories-seed.json`が一つの事実源)、`topics.category`列を追加(それまでJSONにあるだけでDBに永続化されていなかった)。`GET /api/categories`(active問題が1問以上あるカテゴリのみ返す)・`GET /api/topics/random?category=xxx`。Home→「挑戦する」→カテゴリ選択画面(`renderCategoryPicker`)→ランダム or 特定カテゴリで開始
- ~~SPEC.md未作成~~ → 2026-08-15作成済み。今後、仕様に影響する変更は同じコミットで`SPEC.md`も更新すること
- ~~堅牢性: グローバル例外ハンドラ~~ → 2026-08-16、大規模テストで「不正なvoteIdsを送るとサーバーの絶対パス入りスタックトレースがそのままクライアントに漏洩する」実害を確認したため実装。`server/index.js`に4引数のExpressエラーミドルウェアを追加、詳細はサーバーログのみに残しクライアントには汎用エラーのみ返す。fetchタイムアウト処理は引き続き未実装(iOS化前に対応推奨)。クイズ途中状態のlocalStorage復元は実装・実機確認済み
- API認証・レート制限は未実装(公開運用前に要対応)
- **各カテゴリの問題数を20問(active)まで拡充する(2026-08-16指示、絶対対応)**: 現状10〜12問/カテゴリ。取捨選択で削られる分も見込み、**24問前後を目安に実データ収集**する。全11カテゴリ対象、6カテゴリの実データ収集(下記)と同じ手順(WebSearchで一次調査→2択に正規化→sourceに調査主体・年・URL明記)を踏襲する。Part1(固定10問)+Part2(残りからランダム10問)の構造上、20問に届いて初めてPart2が出現するため、この対応が完了するまでは実質全カテゴリPart1のみの状態が続く
- iOS化(Capacitorのリモート URL型)+App Store提出に向けて進行中(2026-08-16〜)。詳細は下記「iOS化・App Store提出に向けて」参照
- 広告(Google AdMobバナー)実装: 収益目的ではなく実装練習として位置づけ。App Store初回提出には含めず、承認後のアップデートで追加する方針(ATT同意プロンプト実装が絡み初回審査のリスクを上げるため)

**6カテゴリの実データ収集(2026-08-15夜)**: WebSearchツールで各カテゴリのテーマに合う一次調査(PR TIMES掲載のプレスリリース・調査会社の公開レポート等)を検索し、実際の割合データに基づいて2択問題を作成する運用で44問を追加した。第三の選択肢(「どちらでもない」等)がある場合は、2択の比率のみを取り出して正規化(按分)する手法(既存の`hamigaki-asa-timing`等と同じ)を踏襲。ソースが見つからなかったテーマ(茶柱・風水・コックリさん・流れ星の願い事等)は無理に埋めず見送った。今後カテゴリを追加・拡充する際も同じ手順(実データ検索→2択に正規化→sourceに調査主体・年・URL明記)を踏襲する。

## iOS化・App Store提出に向けて(2026-08-16〜検討開始)

**目的**: ユーザー本人の転職活動における実績(「iOSアプリを一気通貫で作った」)としての価値が主目的で、DL数・収益は主目的ではない(トラフィック0でも投資対効果ありと判断)。

**技術方針の決定事項**:
- Capacitorは**リモートURL型**(WebViewが常時サーバーの実URLを開く方式)を採用する。埋め込み型(静的アセットをバイナリに焼き込む)は「再ダウンロードなしで問題・カテゴリを追加したい」という要望と両立しないため不採用
- リモートURL型を選ぶ以上、**常時稼働する公開ホスティングが前提**になる。Oracle Cloud Always Free VPS(無料)を使う方針。これは以前保留にしていた「有料ホスティングは検討しない」という判断とセットで動く話
- 広告(Google AdMobバナー)は収益目的ではなく実装練習。**App Store初回提出には含めず、承認後のアップデートで追加**する(ATTのプロンプト実装が絡み、初回審査の複雑さ・リジェクトリスクを上げるため)
- 友人・知人にクリック/インプレッションを稼いでもらう行為は、AdMobの規約(Invalid traffic policy、`clicks or impressions`両方が対象)で明確に禁止されており、発覚するとアカウント永久停止+それまでの正当な収益も没収されるリスクがある。**練習目的であっても絶対にやらない**

**App Store審査を通りやすくする観点で優先すべきこと**(通常の機能優先度とは別軸):
1. 常時ホスティングの安定稼働(審査官が実際に触るため、提出時にサーバーが落ちていると即Reject)
2. プライバシーポリシーのURL(提出必須)、App Store ConnectのApp Privacy申告を実態(年代・性別・血液型・利き手・匿名voterId等の収集)と一致させる
3. Guideline 4.2(Minimum Functionality)対策として、既存のPWA的な作り込み(独自アイコン・スタンドアロン表示・GSAP演出)を維持・強化する
4. バグの完全な潰し込み(Guideline 2.1 App Completeness)
5. API認証・レート制限は審査官のアクセスを誤ってブロックしない設定にする

**現実的な期待値(ユーザーに共有済み)**: App Store経由の自然検索流入はほぼ0(実績・レビューがないため)。X(フォロワー約200人)経由の流入がメインで、1投稿あたり現実的には1〜5DL程度と見積もっている。DL数そのものは目的ではないため、これは許容範囲としてユーザーも合意済み。

**初回リリースのスコープ**: カテゴリは11個中**食事・お金・恋愛・季節行事・健康カラダの5個に絞って**リリースし、後から徐々に追加していく方針(2026-08-16決定・実装済み)。`categories`テーブルに`launched`列を追加(`server/data/categories-seed.json`が一つの事実源、`launched: false`の6カテゴリは`GET /api/categories`・`GET /api/topics`・カテゴリ非指定の`GET /api/topics/random`のいずれにも出ない)。データ自体は削除せず残しているので、公開範囲を広げる際は`categories-seed.json`の`launched`を`true`に変えるだけでよい。カテゴリ指定ありの`/random`(Part1/Part2含む)は`launched`を見ていない(カテゴリ選択画面が`launched`なカテゴリしか提示しないため到達しない、意図的にチェックを省略)

## 技術方針

- **DB必須**: ローカルNode.js + better-sqlite3。DBファイルは`server/data/heibon-vote.db`(gitignore対象)。スキーマは`server/db/schema.sql`が一つの事実源
- サーバー: `server/index.js`(Express)が静的配信(`www/`)とAPI(`/api/*`)の両方を担う。`npm run dev` → `node server/index.js`、ポート4322(他プロジェクトkusutto-games:4321等との衝突回避のため据え置き)
- フロントは引き続きビルドツールなしVanilla HTML/CSS/JS(ESモジュール)。配色・質感トークン(`www/css/style.css`)は`kusutto-games`/`versant-practice`に合わせた近白背景`#faf9f7`+インディゴ`#5b5bd6`、フラット、`scale(0.96)`押下フィードバック
- 多数派判定の同数タイブレークは`options.sort_order`が小さい方を採用(決定的、乱数不要)
- 平凡度ランク・通算称号のしきい値は`server/tiers.js`に集約。現行の値は初回実装時の提案値であり、実際に遊んでみて閾値の体感が合わなければ調整する
- UI目視確認用に`chromium`+`fonts-noto-cjk`を`postCreate.sh`でインストール済み(素のイメージはCJKフォント無しでテキストがtofu表示になるため)
