# heibon-vote プロジェクト固有メモ

「真の平凡投票アプリ」の属性別集計版。お題(選択肢式)に投票してもらい、年代・性別などの属性別に集計結果を見せるWebアプリ。まずWebで動くものを作り、将来的にiOS化(まずPWA、必要ならApp Store配信)を検討する。

共通の開発標準は `~/.claude/CLAUDE.md`(postCreateCommandで自動配置)を参照。ここにはこのプロジェクト固有の制約・quirksのみ書く。

## 現状(2026-08-04)

雛形のみ作成。要件(お題の持ち方、属性の種類、集計ロジック、初期データ)は未確定。実装着手前にPlanモードで方針を決める。

母集団は当面ダミーデータで代替する方針(実データ収集の当てが未定のため)。

## 技術方針(暫定)

- devcontainerはNode.jsベース(`kusutto-games`と同一パターン)。ビルドツールなしのVanilla HTML/CSS/JSを想定(要件確定時に見直す可能性あり)
- 開発サーバー: `npm run dev`(`serve www -l 4322`)。ポート4322は他プロジェクト(kusutto-games:4321等)との衝突を避けるため採番
