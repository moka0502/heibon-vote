# レビュー結果(2026-08-22 夜間・多ペルソナ+自動監査)

実機FB3件をきっかけに、TEST-PLAN.mdの層1〜5を初回実行した結果。
**この内容は、UX-BACKLOG.md の未コミット変更が片付いたら、そちらへ統合してよい。**

---

## A. 今夜すでに直したもの(デプロイ済み)

| # | 内容 | commit |
|---|---|---|
| 1 | ヘッダーがステータスバーに潜り込む(safe-area-inset-top漏れ) | `764862d` |
| 2 | 1問も答えず離脱しても「挑戦済み」になる(記録タイミング) | `486f14f` |
| 3 | プライバシーポリシーだけアイコン無し | `cc8ee82` |
| 4 | 「戻る」のタップ領域が幅32px(HIG 44x44未満) | `fde370d` |
| 5 | 結果画面の「履歴を見る」だけアイコン無し | `fde370d` |

## B. 良好だったもの(記録として)

自動テストで確認済み。**ここは今後も回帰として守る価値がある**。

- サーバー側の入力検証 **13/13**。空・空白のみ・10万字・型違い・存在しないID、すべて4xxで弾き**500に落ちない**
- **XSS安全**。`<img src=x onerror=alert(1)>` を投稿してもalertは出ず、生タグもDOMに入らない
- **横スクロールなし**。超長文字列(2000字)・文字サイズ2倍・横画面のいずれでも発生せず
- 送信ボタン5連打・画面間8往復で**JSエラー0件**
- `prefers-reduced-motion` 対応あり
- **コントラスト不足の検出ゼロ**(WCAG AA基準で全画面走査)
- 横画面でもレイアウトが破綻しない

---

## C. 未対応のFB(要判断)

### C-1. 一貫性 / UI
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **「ホームに戻る」(4画面)・「戻る」(2画面)・「クイズを中断する」にアイコンが無い**。今夜アイコンを揃えたが、これらは home / arrow-left など**新規アイコンの意匠決定が要る**ため保留 | 中 | 低 |
| b | ホームで**「履歴を見る」だけ下線なし・太字**(`btn-link-emphasis`)。意図的な強調だが、5つ並ぶと不揃いに見える。強調を残すか揃えるかの判断 | 低 | 低 |
| c | **プライバシーポリシーが機能リンクと同列・同ウェイト**。法務系フッター項目なので、小さく・下に離す方が情報階層として自然 | 中 | 低 |

### C-2. アクセシビリティ
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **結果画面の平凡度スケールラベルが11.2px**(唯一無二/個性派/バランス派/平凡寄り/真の平凡)。日本語では小さい | 中 | 低 |
| b | **アイコンSVGが `width="20" height="20"` 固定**で、文字サイズ拡大に追随しない。文字2倍時にアイコンだけ極端に小さく見える。`em`指定にすれば連動する | 中 | 低 |
| c | **ダークモード未対応**(`prefers-color-scheme` のルール0件、背景は常に `rgb(250,249,247)`)。iOSでは非対応だと古く見える。審査の落ちる理由にはならない | 中 | 高 |

### C-3. コピー(コピーライター視点)
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **「挑戦済み」という語**。完走時のみ付くよう直したので実態とは合ったが、「回答済み」「クリア済み」の方が正確か検討余地 | 低 | 低 |
| b | ホームの見出し**「挑戦しよう」とボタン「挑戦する」が近接して重複**。見出しを別の切り口にすると情報量が増える | 低 | 低 |
| c | **「お題の内訳を見る」の「内訳」が硬い**。デジタル不慣れなユーザーには「どんなお題があるか見る」等の方が伝わる | 中 | 低 |
| d | **「Part1」「Part2」の英語**。「その1」「その2」の方が親切な可能性。ただし説明文は既に画面にある | 低 | 低 |

### C-4. 片手操作 / レイアウト
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **クイズの選択肢が画面上部〜中央(y≈268/332)にあり、下半分(y>440)が空白**。親指が届きやすいのは下方なので、選択肢を下に寄せると片手で押しやすい。**このアプリは10問連続でタップする設計なので効果が大きい** | 高 | 中 |
| b | **クイズ中もアプリヘッダー(タイトル+タグライン)が常時表示**で約90px消費。クイズ中は縮める/隠すと本文に集中できる | 中 | 低 |

### C-5. カテゴリ選択 / グロース
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **「食事 Part1」「食事 Part2」とカテゴリ名が重複して10行**並ぶ。カテゴリごとにグループ化(食事 [1][2])すれば高さが半分になり走査しやすい | 中 | 中 |
| b | **未公開6カテゴリに「近日公開」を出す**と期待醸成になる(マーケ視点)。現在は存在自体が見えない | 中 | 低 |

### C-6. パフォーマンス(実測値)
初回ロード: **FCP 964ms / load 1781ms / 総転送量 158KB**。全体としては軽い。

| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **`gsap.min.js` が72KBで総転送量158KBの46%を占める**。最大の資産が演出ライブラリ。使っている演出がCSS animationで置換できるなら、転送量をほぼ半減できる | 中 | 中 |
| b | **起動スプラッシュが消えるまで2685ms**。FCPは964msなので、**その後1.7秒は紫の画面を見ているだけ**。スプラッシュは最初のmount()で消える設計で、API取得を待っている。先に骨組みを出す(スケルトン)か、スプラッシュ中に進捗を見せると体感が改善する | 中 | 中 |

参考: 最初の1問に到達するまで**2タップ/1.1秒**。導線自体は短く、せっかちなユーザーにも十分。

### C-7. プライバシー / 法務
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **お題提案フォームに「個人情報は書かないでください」の注意書きが無い**(`個人情報`等の語がUIに0件)。唯一の自由入力欄なので、ユーザーが本名や連絡先を書くと**意図せず個人情報を預かることになる**。今日のリポジトリPIIの議論と同じ筋の話 | 高 | 低 |
| b | **プライバシーポリシーにデータの保存期間の記載が無い**(該当語0件)。アクセスログは「一時的に記録」とあるが期間が不明。回答データの保持期間も未記載 | 中 | 低 |

良かった点: ログイン無し・氏名/メール/電話を取得しない・Cookieトラッキング無し・第三者提供無し・
問い合わせは個人メアドではなくアプリ内フォーム、と**設計思想は一貫して保守的**。
App Store Connectの App Privacy 申告とこの内容が一致していることは、提出時に再確認すること。

---

## D. まだ実施していない層

- **層3(実機チェック9項目)**: セーフエリア以外は未確認。**TestFlightの実機でしか取れない**ためユーザー作業
- 層4の「App Store審査官」ペルソナ: ガイドライン4.2の観点は、実機の触り心地の評価が要るため実機確認とセットで

---

# 2周目(修正後の再テスト・再レビュー)

## 結果

| テスト | 結果 |
|---|---|
| 回帰テスト `npm test` | **21/21 成功**(未公開カテゴリの回帰9件を追加) |
| 全画面監査(8画面) | **指摘ゼロ**(アイコン欠落・タップ領域・小さい文字・コントラスト) |
| 意地悪・モンキーテスト | **13/13 OK** |
| 環境系(文字2倍・横画面・アニメ抑制・オフライン・細い回線) | 7/8 OK。NGはダークモードのみ(**除外指示済み**) |

1周目に挙げた指摘のうち、**ダークモードを除くすべてを解消**した。

## 1周目からの修正で解消したもの

- アイコン欠落7箇所 → `home`/`back` を定義し全画面で統一。**根本原因は `icon('home')` が
  ICONS未定義のまま呼ばれ、`?? ''` で黙って空を返していたこと**。`icon()` を fail loudly 化した
- アイコンが文字サイズ拡大に追随しない → SVGを `em` 指定に変更
- 結果画面のスケールラベル11.2px → 360px以上でのみ0.75remへ(320px幅で詰まる既知問題を再発させないため)
- プライバシーポリシーの情報階層 → `btn-link-legal` で小さく薄く下に離した
- 選択肢が画面上部 → クイズ中はヘッダーを畳み、カードを縦に伸ばして選択肢を下端へ。
  **実測でiPhone14相当(390x844)では選択肢2つとも画面下半分に入る**(変更前は0個)
- カテゴリ選択の10行 → カテゴリ名1回+束を横並びに
- 文言4件(回答済 / あなたは何問、多数派? / どんなお題があるか見る / その1・その2)
- 提案フォームの個人情報注意書き、プライバシーポリシーの保存期間

## 2周目で新しく見つかったもの → **両方とも対応済み**(commit `caff89e`)

| # | 内容 | 対応 |
|---|---|---|
| a | 「挑戦しました」と「回答済」で語が混在していた | 結果画面の見出しを**「〜のお題に回答しました」**に変更し、バッジの「回答済」と揃えた |
| b | 結果画面の「くわしい内訳を見る」に「内訳」が残っていた | **「1問ずつの結果を見る」**に変更(結果画面・履歴詳細の2箇所)。中身をそのまま言い表す表現にした |

これで**利用者に見える「内訳」という語は無くなった**(コード内のコメントには残る)。

## 3周目(文言修正後の確認)

- 回帰テスト **21/21 成功**
- 全画面監査(8画面) **指摘ゼロ**
- 実画面で確認: 「食事 その1」のお題に**回答しました** / **1問ずつの結果を見る** /
  完走時 `playedParts: ["food:1"]` / JSエラー0件

**未対応として残るのは、意図的に残した4件のみ**(ダークモード / 未公開カテゴリの「近日公開」 /
gsap置換 / スプラッシュ短縮)。

## 未対応のまま残したもの(理由つき)

| # | 内容 | 残した理由 |
|---|---|---|
| a | **ダークモード対応** | ユーザー判断で除外 |
| b | **未公開カテゴリの「近日公開」表示** | 出題APIが `launched` を見ていない潜在バグを先に塞いだので**実装可能になった**。ただし「何が来るか見せる」のは製品/ロードマップの判断であり、不具合ではないため手を出していない |
| c | **`gsap.min.js` 72KB(総転送量の46%)の置換** | 演出の作り直しになり、視覚回帰の確認は実機でないとできない。App Store提出直前に入れる変更としてリスクが高い |
| d | **起動スプラッシュ2685msの短縮** | スプラッシュは最初の `mount()` で消える設計で、短縮にはAPI取得を待たずに骨組みを出す構造変更が要る。cと同じ理由で見送り |


---

# 4周目(2026-08-22 夕方): 「いつものテスト」+ 残り8ペルソナ

Docker Desktop再起動でコンテナがexit 255になった件は `docker start` のみで復帰(破損なし)。

## 「いつものテスト」の結果

昨夜やり残していた観点(**1.5 操作の後始末** / 境界値 / セレクト連打 / リーク / 0件表示)を実施。

| 分類 | 件数 | 結果 |
|---|---|---|
| 意地悪(境界値・不正入力・重複実行) | 16 | **15 OK / 1 NG** ※NGはテスト設計のミス(下記) |
| 後始末・モンキー・ユースケース・0件表示 | 16 | **16 OK** |

- **境界値**: `count=0→1件` `count=101→100件` `count=abc→10件` `count=-5→1件`、
  提案 **500字ちょうど→201 / 501字→400**。すべて意図どおりクランプ・拒否される
- **重複実行**: 同一お題への二重投票は200/200、同一voteIdでのセッション二重作成は400/400。いずれも500に落ちない
- **後始末(1.5)**: 提案の送信後はサンクス画面へ遷移し、disabledのボタンが残らない
- **0件表示**: 履歴0件で「まだ挑戦履歴がありません。〜」と案内が出る。undefined/NaNの露出なし
- **モンキー**: 属性ボタン40回ランダム連打・画面間8往復でJSエラー0件
- **リーク**: `setInterval` は不使用。`createObjectURL` は下記FBあり
- **実データ**: 開発DBの全9テーブルが**開始前と完全一致(差分0)**。隔離できている

**NG1件はアプリではなくテスト側の問題**: 提案APIを7回連続で叩いたため6回目以降が
レート制限(60秒5回)で429になり、検証したかったバリデーション結果が隠れていた。
5回以内に収めて測り直し、全項目を確認した。**同時にレート制限が正しく効くことの確認にもなった**。

## 見つけて直した不具合: サーバーの案内が画面に届いていなかった

**再現**: 提案フォームから6回連続で送信する(60秒5回制限に当たる)。
**期待**: 「アクセスが集中しています。しばらく待ってから再度お試しください。」
**実際**: 「通信がうまくいきませんでした。もう一度お試しください。」+「再読み込み」ボタン

原因は `renderError` がメッセージを常に共通文言へ差し替えていたこと。技術的な詳細を
漏らさない意図は妥当だが、サーバーのエラー文言14件のうち**13件が英語の技術文言、
1件だけが利用者向けの日本語**で、後者まで巻き添えになっていた。
利用者からは原因も対処も分からず、再読み込みを押しても60秒経つまで直らない。

**修正(commit `63f5e97`)**: サーバー側で `userFacing: true` を立てたメッセージだけ
画面に出す契約にした。回帰テストで「英語の技術文言にはuserFacingが付かない」ことと
「429がuserFacing付きの日本語で返る」ことの両方を確認する(`npm test` 23/23)。

## 残り8ペルソナのFB

### リピーター / やりこみ勢 ★構造的な指摘
実測: **「その1」は2回引いても完全に同一の10問**(固定10問の仕様どおり)。
「その2」も残りが10問しかないため実質固定。ランダム10問を2回引くと**1/10問が重複**。
遊べる母集団は**5カテゴリ × 束10個 = 最大100問**。

| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **一度遊んだカテゴリは次も全く同じ10問**。リピーターは2周目から新しい体験が無い。未公開6カテゴリ(計103問)を出せば倍増するが、それでも有限 | 高 | 中 |
| b | **毎日開く理由が無い**。デイリーお題・週替わりなどの再訪動機がない | 中 | 中 |
| c | **カテゴリ制覇の進捗が見えない**。「回答済」バッジのみで、全体の何%かが分からない | 中 | 低 |

### カスタマーサポート
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **不具合報告の導線が無い**。問い合わせ先はお題提案フォームだが、これは「お題のアイデア」用途と明記されている。バグ報告をどこに書けばいいか分からない | 中 | 低 |
| b | **エラー時に入力テキストが失われる**。エラーは画面ごと遷移する作りなので、長文を書いた提案がレート制限等で消える。実測で確認済み | 中 | 中 |

### 日本語表現 / ローカライズ
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **敬体と常体が混在**。ホームは常体(「〜試そう」)、オンボーディングは敬体(「〜試してみましょう」)、提案フォームは敬体(「〜お送りください」)。画面ごとに語り口が変わる | 中 | 低 |
| b | **「挑戦」と「回答」が混在**。「回答済」「〜のお題に回答しました」に対し、「挑戦する」「もう一度挑戦する」「まだ挑戦履歴がありません」「これまでの挑戦者」は「挑戦」のまま。**これは今回の変更で生じた面がある**(バッジと結果見出しだけ「回答」に寄せたため)。どちらかへ全面的に寄せるか、役割を決めて使い分けるかの判断が要る | 中 | 低 |

### ブランド
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | 「平凡」というコンセプトは一貫している(「『平凡』は、実はすごい」/称号5段階)。一方で**「多数派」「共感性」「王道」など説明的な語**が混じる。ブランドワードに寄せる余地 | 低 | 低 |
| b | 結果画面の「これまでの挑戦者N人中、あなたより『共感性が高く、定番を理解し、万人受けする王道』だった人はM人でした」が**一文として長く読みづらい**。ブランドの声というより説明文 | 中 | 低 |

### データアナリスト
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **計測が一切ない**。どのカテゴリが人気か、何問目で離脱するかが分からない。votes/quiz_sessionsから事後にSQLで追えるが、**中断イベントは記録されないため離脱点が分からない** | 中 | 中 |

### 子ども・若年層
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | **「二択」「属性」「算出」「傾向」「該当」など硬めの語**が残る。「属性別の傾向」は特に難しい | 中 | 低 |

### マーケター / グロース
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | 結果画面の「これまでN人が挑戦しています」は、**Nが小さいうちは逆効果**(例: 3人)。閾値を設けて出し分ける手がある | 中 | 低 |

### 実装上の小さな穴(リーク)
| # | 内容 | 効果 | 難易度 |
|---|---|---|---|
| a | `URL.revokeObjectURL(url)` が `a.click()` の後ろにあり、**`a.click()` が例外を投げると解放されない**(コメント上も a.click() は投げうる前提)。`finally` で解放するのが確実 | 低 | 低 |

---

## 4周目のFB対応(ユーザー判断を受けて実施)

判断: **語り口は常体に統一** / **硬い語はやさしく**。

| # | FB | 対応 |
|---|---|---|
| 5 | エラー時に入力テキストが失われる | **対応**。フォームに留まって理由を出し、入力を残し、ボタンを操作可能に戻す。回帰テスト4件を追加 |
| 6 | 敬体と常体が混在 | **対応**。31箇所を常体に統一。残る敬体0件を確認。**`privacy.html` は法務文書のため敬体のまま** |
| 9 | 結果画面の一文が長すぎる | **対応**。「これまでの挑戦者N人のうち、あなたより平凡だったのはM人。」に短縮しブランドワードへ寄せた |
| 11 | 硬い語(二択・属性・算出・該当) | **対応**。非コメント行から4語とも消えたことを確認 |
| — | ObjectURLのリーク | **対応済み**(commit `2d59965`) |

### 見送ったもの

| # | FB | 理由 |
|---|---|---|
| 7 | 「挑戦」と「回答」の混在 | **私が過剰に挙げた可能性が高い**。見直すと「挑戦＝する行為 / 回答＝答えた記録」の役割分担として成立しており、無理に統一すると「挑戦する」というCTAの力が弱まる |
| 12 | 「これまでN人が挑戦しています」の閾値 | **2026-08-18のマーケレビューで「母集団が薄い初期でも人数は出す」と決めた記録がコード内コメントに残っている**。黙って覆すのは不適切なため、判断を仰ぐ |
| 1,2,3,10 | リピーター施策・進捗可視化・計測 | 新機能のためPlanを挟む |
| 4 | 不具合報告の導線 | 「どこで受けるか」の製品判断が必要 |
| 8 | ブランドワードへの語彙寄せ | #9で長文は解消。残りは好みの領域 |

### 検証
- `npm test` **27/27 成功**(回帰テストを23→27件に拡張)
- 全画面監査(8画面) **指摘ゼロ**
- 実測: 送信失敗時に**入力54/54文字が残り**、理由が表示され、ボタンは `disabled=false` に復帰
