# Plan: リピーター施策・進捗可視化・計測

作成: 2026-08-22。多ペルソナレビューのFB #1/#2/#3/#10 に対する実装計画。
**まだ着手していない。方針の合意を得てから実装する。**

## 1. 問題の構造(実測)

| カテゴリ | 公開 | 問題数 | 束 | 20問まであと |
|---|---|---|---|---|
| 食事 / お金 / 恋愛 / 季節行事 / 健康・カラダ | 公開 | 各20 | 各2 | 0 |
| 住まい・身支度 / 余暇・娯楽 / 仕事・働き方 | 未公開 | 各18 | 各1 | 各2 |
| デジタル習慣 / 持ち物 | 未公開 | 各16 | 各1 | 各4 |
| ジンクス・運試し | 未公開 | 17 | 1 | 3 |

- **公開中は 10束 = 100問**。「その1」は固定10問なので、**一度遊んだ束は次も完全に同じ10問**
  (実測: 同じIDが同じ順で返る)。「その2」も残りが10問しかなく実質固定
- ランダム10問を2回引くと1/10が重複
- **つまり熱心な人ほど早く在庫が尽き、再訪する理由が無くなる**

## 2. この問題の着眼点

このアプリの価値は「自分がどう答えたか」ではなく **「みんなの分布とどれだけ一致するか」** にある。
**同じ10問でも、母数が増えれば多数派は変わりうる。**
つまり「同じ問題をもう一度解いて、みんなの答えが変わったか見に来る」という再訪動機が
理屈の上では成立する。これは在庫を増やさずに再訪を作れる、このアプリ固有の切り口。
ただし**母数が増えないと結果が動かない**ので、利用者が少ない初期には効かない。

## 3. 打ち手の候補

| # | 施策 | 得られるもの | コスト | リスク・前提 |
|---|---|---|---|---|
| A | 未公開6カテゴリをそのまま公開 | +6束(60問)、計160問 | **極小**(launchedフラグ。APIの穴は8/22に塞いだ) | 各1束しかなく「その1」だけで中途半端に見える |
| **A'** | **17問足してから6カテゴリを公開** | **+12束(120問)、計22束220問(2.2倍)** | **問題を17問書くだけ。コード変更ほぼ無し** | 執筆コストのみ |
| B | デイリーお題(日付シードで毎日10問) | 毎日の再訪動機 | 中(出題APIに日付シード経路+UI) | 在庫が少ないと同じ問題がすぐ回る。A'の後が前提 |
| C | 制覇の進捗可視化 | やりこみの目標 | **小**(既存の`playedParts`から算出できる) | 束が10しかないと「すぐ終わる」感が出る。A'の後が望ましい |
| D | 計測(離脱点・人気カテゴリ) | 改善の材料 | 中 | **プライバシーポリシーとの整合が必要**(下記) |
| E | 「前回との差分」表示 | 上記2.の独自性を活かす | 大(過去回答の保持・比較UI) | 母数が増えるまで効果が出ない |

## 4. 推奨する順序

### 第1段階: A'(17問追加 → 6カテゴリ公開) ★最優先
**新機能をひとつも作らずにコンテンツが2.2倍になる。** 費用対効果が突出している。
- やること: `questions-seed.json` に17問追加 → `categories-seed.json` の `launched` を1に
- 影響範囲: データのみ。コード変更なし
- 確認: `npm test` の未公開カテゴリ回帰テストは**公開後に期待値を変える必要がある**
  (現在は home-routine/leisure が0件であることを検証しているため)。ここは要注意

### 第2段階: C(進捗可視化)
束が22に増えた状態なら「制覇」が現実的な目標になる。
- やること: ホームか カテゴリ選択に「22束中 N束クリア」を出す。`getPlayedParts()` の件数で足りる
- 影響範囲: 表示のみ。データ構造の変更なし

### 第3段階: B(デイリーお題)
在庫が220問になってから。日付をシードにして全問題から10問選ぶ。
- やること: 出題APIに `mode=daily` を足し、日付で決定的にシャッフル
- 影響範囲: 出題API + ホームの導線。既存の束の仕組みは触らない

### 見送り: D(計測)・E(差分表示)
- **D は privacy.html との整合が先**。現ポリシーは「Cookieによるトラッキングは行わず、
  端末IDの保存にはlocalStorageのみを使用」「行動追跡(トラッキング)や広告目的には一切使用しません」
  と明記している。**外部アナリティクスを入れるとポリシー改定とApp Privacy申告の変更が要る**。
  一方 **中断イベントをサーバー側に記録するだけ**なら、既に取得している回答データの範囲に近く、
  ポリシーの書き換えが小さく済む可能性がある。まずそこだけ切り出すのが現実的
- **E は母数が増えてから**。今やっても結果が動かず、体験が成立しない

## 5. 決めてほしいこと

1. **第1段階(A')に進んでよいか。** 17問は私が起案してレビューしてもらう形でよいか、
   それとも masakさん自身が書くか(お題の面白さはアプリの中核なので、ここは判断が要る)
2. 第2段階(C)まで一気にやるか、A'の効果を見てから決めるか
3. Dは「中断イベントのみ」に絞って検討する方向でよいか
