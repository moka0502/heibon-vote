# よくあるアプリバグ チェックリスト(汎用版)

heibon-voteの大規模テスト(モンキー/閾値/意地悪/ペルソナ/セキュリティ)で見つかった不具合をもとに作成した、汎用的なチェックリスト。個人開発(Node.js/Express + SPA的フロント、DBあり、Web API公開)のプロジェクトであれば概ねそのまま使い回せる想定。新規プロジェクト・大きめの機能追加のタイミングで、該当する項目だけでも目視 or 実地テストすることを推奨する。

作成: 2026-08-16。heibon-voteでの実施結果は`git log`参照(該当コミット: 丸め誤差修正・二重送信修正・X-Frame-Options追加・型検証追加など)。

チェック方法の凡例: 「✅」は実際にブラウザ操作/API叩きで確認したもの、「📖」はコードリーディングで判断可能なもの。

## 1. 入力検証
- [ ] 必須フィールド欠如(null/undefined/空文字)で適切に4xxを返すか
- [ ] 数値であるべき箇所にオブジェクト・配列・文字列を渡した時の挙動(500に落ちないか)
- [ ] 配列であるべき箇所に別の型、配列の要素の型が不揃いな時の挙動
- [ ] 長大な文字列・巨大なJSONペイロードの上限(サイズ制限・413)
- [ ] 空文字/空白のみの送信(トリムしてから検証しているか)
- [ ] 絵文字・サロゲートペア・RTL文字・制御文字などUnicode特有の入力
- [ ] 数値の桁あふれ(Number.MAX_SAFE_INTEGER超、負の数、NaN、Infinity)
- [ ] `x || デフォルト値`のようなfalsy値の罠(0や空文字が意図せずデフォルトにフォールバックしないか)
- [ ] クエリパラメータの型(文字列として届くため、数値変換の失敗ケース)

## 2. 認証・認可・アクセス制御
- [ ] 他ユーザーのリソースが横断的に見えていないか(ユーザー識別列がなく全件返しているAPIがないか)
- [ ] IDの使い回し・再利用によって他人のデータが書き換わらないか
- [ ] 連番IDでの列挙(enumeration)によって他人のデータの存在・状態を推測できないか
- [ ] エラーメッセージの粒度が違いすぎて「存在しない」と「権限がない」を区別できてしまわないか
- [ ] API認証・トークンなしで重要な操作(投稿・投票・課金等)ができてしまわないか
- [ ] レート制限なしで連打・スクリプトによる大量リクエストが可能でないか
- [ ] CSRF対策(認証がCookieベースの場合は特に)
- [ ] クリックジャッキング対策(X-Frame-Options / CSP frame-ancestors)
- [ ] SQLインジェクション(生SQL文字列結合をしていないか、prepared statement/ORMを使っているか)
- [ ] パストラバーサル(静的ファイル配信・ファイルアップロード機能がある場合)
- [ ] プロトタイプ汚染(`__proto__`等をJSONキーとして受け取る箇所がないか)

## 3. データ整合性
- [ ] 複数の値を独立に丸めて(Math.round等)合計・パーセンテージ表示する箇所で、合計が期待値からずれないか
- [ ] 同一リクエストの二重送信でレコードが重複作成されないか(冪等性)
- [ ] 存在しない外部キー(topicId等)を指定した時に適切にエラーになるか
- [ ] 並行リクエストでのレース条件(TOCTOU): 同時に同じリソースを取り合った時の挙動
- [ ] 集計のしきい値・境界値(ちょうど閾値の件数の時の挙動)
- [ ] 不要になったデータ(移行済みの旧データ等)のクリーンアップ漏れ
- [ ] 重複投稿・重複回答の扱い(同一ユーザーの複数回答をどう集計するか)
- [ ] NULL/旧データの扱い(移行前のレコードが集計ロジックで想定通り扱われるか)
- [ ] サンプル数が少なすぎる集計結果をそのまま見せていないか(信頼できる最低件数の設定)
- [ ] ステータス変更(公開→非公開等)が過去の関連データの表示を壊さないか

## 4. 状態管理・UI
- [ ] リロード・タブ閉じ→再訪問で進行中の状態が消えないか(または意図通り消えるか)
- [ ] 離脱後に古い進行状態へ強制的に戻されないか
- [ ] ブラウザの戻る/進む、スワイプバックが画面遷移と整合しているか
- [ ] ボタン連打・二重タップでAPI呼び出しが多重発火しないか(disabledガードの有無)
- [ ] ローディング中の操作(二重送信・二重遷移の抑止)
- [ ] 複数タブを同時に開いた時のlocalStorage競合
- [ ] 非同期レスポンスの順序逆転(古いレスポンスが後から届いて画面を巻き戻さないか)
- [ ] フォーム入力中の意図しない画面遷移・データ消失
- [ ] アニメーション中の連打によるDOM状態不整合
- [ ] スクロール位置が画面遷移間で意図通り保持/リセットされるか

## 5. ネットワーク・オフライン
- [ ] オフライン時に専用の案内画面が出るか(白画面・無限ローディングにならないか)
- [ ] 通信エラー時にユーザー向けの分かりやすい文言が出るか(技術的なエラー詳細を露出していないか)
- [ ] fetchのタイムアウト処理(応答がいつまでも返らない場合の挙動)
- [ ] Service WorkerがAPIレスポンスを誤ってキャッシュしていないか(常に最新であるべきデータ)
- [ ] Service Workerのバージョン更新時に古いキャッシュが残り続けないか
- [ ] オフライン中に書き込み系操作(投稿・投票)をブロックしているか
- [ ] リトライ処理の有無(一過性エラーへの対応)
- [ ] PWAマニフェストの妥当性(JSON形式・アイコン参照切れ)
- [ ] Service Worker登録失敗時のフォールバック
- [ ] 通信の遅延・パケットロスをシミュレートした時の体感

## 6. ブラウザ・デバイス互換性
- [ ] 実ブラウザでのズーム操作(pinch-zoom)での表示崩れ(CSS `zoom`プロパティでの疑似テストは実機と乖離しやすいので注意)
- [ ] 狭いビューポート(iPhone SE相当等)での表示崩れ
- [ ] `prefers-reduced-motion`等のアクセシビリティ設定への対応
- [ ] 日本語(CJK)フォントが確実に表示される環境か(検証環境にCJKフォントが入っているか)
- [ ] Safari固有の挙動(特にPWA・localStorage・日付処理まわり)
- [ ] タップ領域の広さ(最低44px目安)
- [ ] 数字表示のアニメーション中の字幅ガタつき(tabular-nums等)
- [ ] 横画面(landscape)レイアウト
- [ ] PWAアイコンの解像度・存在確認
- [ ] ダークモード対応の有無(未対応なら明示的に「未対応」と分かる状態か)

## 7. パフォーマンス
- [ ] N+1クエリ(一覧の各要素ごとに個別クエリを投げていないか)
- [ ] データ量が増えた時のAPIレスポンス速度
- [ ] 画像・アイコンのファイルサイズ
- [ ] アニメーション多用によるレンダリング負荷
- [ ] localStorage/sessionStorageの肥大化

## 8. セキュリティ
- [ ] SQLインジェクション(再掲、最重要)
- [ ] スタックトレース・サーバーの絶対パス等がエラーレスポンスに漏れていないか(グローバル例外ハンドラの有無)
- [ ] クリックジャッキング対策(再掲)
- [ ] パストラバーサル(再掲)
- [ ] プロトタイプ汚染(再掲)
- [ ] CORS設定(意図しないオリジンからのAPI呼び出しを許可していないか)
- [ ] レート制限・DoS耐性(再掲)
- [ ] XSS(innerHTML等で未エスケープの外部入力をDOMに挿入していないか)
- [ ] 秘密情報(APIキー・トークン)がコード・ログ・レスポンスに含まれていないか
- [ ] 依存パッケージの既知脆弱性(npm audit等)

## 9. ロジック・計算バグ
- [ ] 丸め誤差(再掲)
- [ ] 同数・タイの場合の決定的な扱い(乱数に頼っていないか)
- [ ] しきい値・ランク分けロジックが実データ分布に対して妥当か(モンテカルロ等での事前検証)
- [ ] カウント系ロジック(通算回数・連続記録等)の境界値
- [ ] パーセンタイル・順位計算のサンプル数不足時の扱い
- [ ] スナップショットのタイミング(「投票前の状態」等、時点を正しく捉えているか)

## 10. その他アプリ特有/PWA
- [ ] Service Workerの`skipWaiting`+`clients.claim`(更新の反映タイミング)
- [ ] マニフェストのJSON妥当性(再掲)
- [ ] プッシュ通知の実装有無・オプトアウト
- [ ] ホーム画面追加時のスプラッシュ・アイコン表示
- [ ] ネイティブラップ(Capacitor等)後に固有の挙動が変わらないか

---

## 使い方メモ

- 全項目を毎回フルで実施する必要はない。新機能追加時は「1.入力検証」「3.データ整合性」「4.状態管理・UI」あたりを中心に、影響範囲に応じて他カテゴリも拾う
- 「1箇所で見つかった不具合パターンは、同じ実装パターンを使っている他の箇所にも横展開して確認する」のが効果的(heibon-voteでは丸め誤差・型検証漏れの2件がこのやり方で追加発見できた)
- グローバル例外ハンドラは「未知の不具合による情報漏洩」を防ぐ最後の砦であって、個別の入力検証の代わりにはならない。両方セットで用意する
