{
  "name": "heibon-vote",
  "private": true,
  "version": "0.1.0",
  "description": "真の平凡投票アプリ(属性別集計)",
  "scripts": {
    "dev": "node server/index.js",
    "backup": "node -e \"console.log(require('./server/backup').backupNow())\""
  },
  "dependencies": {
    "better-sqlite3": "^11",
    "express": "^4",
    "express-rate-limit": "^8.6.2"
  },
  "devDependencies": {
    "gsap": "^3.15.0"
  }
}
