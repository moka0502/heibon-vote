# テスト計画(heibon-vote)

作成: 2026-08-22。実機FBで3件の不具合が出たことを受けて整備。
共通標準「3. 品質保証」を、このアプリで具体化したもの。

## なぜ必要か(今日漏れた3件の分析)

| 見つかった問題 | 漏れた理由 | 対応する層 |
|---|---|---|
| タイトルがステータスバーと重なる | 実機でしか再現しない(ブラウザ375×812では出ない) | **層3: 実機チェック** |
| 1問も答えず離脱しても「挑戦済み」 | 「中断」という状態が観点に無かった | **層2: 状態マトリクス** |
| プライバシーポリシーだけアイコン無し | 一貫性レビューを回していなかった | **層4: 3ペルソナレビュー** |

道具(`usual-test`/`usual-review` Skill・`COMMON-APP-BUGS-CHECKLIST.md`)は既にある。
足りないのは **(a)自動化された回帰の層** と **(b)実機の層** と **(c)実際に回すこと**。

---

## 層1: 自動テスト(回帰防止) ★最優先・現在ゼロ

`npm test` の一発で走ること。外部サービス・秘密情報なしで動き、本番データを触らない。

**実行環境**: devcontainer内 + `/usr/bin/chromium` + playwright-core(導入済み)。
サーバーは `node server/index.js` をテスト用ポートで起動し、終了時に落とす。

**決定性の担保**:
- プロフィールは `localStorage` に固定値をseedしてオンボーディングを飛ばす
  (実績のある形: `addInitScript` で `heibonVote.profile` を直接セット)
- 起動スプラッシュ・画面遷移アニメの分、描画待ちを入れる(実測3.5秒で安定)
- **DBは使い捨て**。`server/data/` をテスト用にコピーして使い、終了後に消す

**最初に入れるケース(今日の2件を回帰として常設)**:
1. 1問も答えず離脱 → 「挑戦済み」バッジが付かない(`playedParts`が未記録であること)
2. 完走 → 「挑戦済み」バッジが付く
3. ランダム挑戦で完走 → **どのカテゴリにもバッジが付かない**
4. ホームの全リンクがアイコンを持つ(一貫性の機械チェック)
5. 主要APIが200(`/` `/api/topics` `/api/categories` `/api/attributes` `/privacy.html`)

**注意(実際に踏んだ罠)**:
- リロードすると**途中のクイズがあればホームを飛ばして再開する**(`app.js:382`)。
  離脱を再現するには `localStorage.removeItem("heibonVote.quizState")` が要る
- `locator(sel, { hasText })` は動かなかった。`.first()` 等の素直な指定にする
- Service Worker登録は自動化ブラウザで失敗する。**アプリのバグではない**

## 層2: 状態マトリクス(今回の②を体系的に取る)

「開始」と「完走」だけでなく、**中断・再開・失敗**を必ず観点に入れる。

| 状態 | 確認すること |
|---|---|
| 未開始 | バッジ無し・履歴0件の表示が破綻しない |
| 開始直後(0問回答) | **記録が一切残らない** |
| 途中(1〜9問) | 離脱→再開で続きから戻る。記録はまだ残らない |
| 完走 | 記録される・結果が出る・履歴に入る |
| 送信失敗 | 進行状況が消えない(`app.js:292`にその配慮のコメントあり) |
| 完走後の再挑戦 | 二重記録されない |

## 層3: 実機チェック(ブラウザでは絶対に取れない) ★今回の①はここ

TestFlightのビルドをiPhone実機で確認する。**リリース前に必ず1周**。

- [ ] **セーフエリア**: タイトル等がステータスバー(時刻・電池)と重ならない
- [ ] **セーフエリア下**: ボタンがホームインジケータに被らない
- [ ] 横画面にしても崩れない
- [ ] iOSの文字サイズ設定を最大にしても破綻しない
- [ ] ダークモード/ライトモードの両方
- [ ] 機内モード(オフライン)で開いたときの挙動
- [ ] バックグラウンド→復帰でクイズが継続する
- [ ] 触覚フィードバック(Haptics)が効く
- [ ] **起動して真っ白にならない**(リモートURL型の最大リスク)

## 層4: 多ペルソナレビュー(`usual-review` Skillを拡張)

従来はUX/CX/マーケの3人だった。今日の実機FBで「観点が足りない」と判明したため25人に拡張し、
回す頻度で3段に分けた(2026-08-22)。

### コア5(変更のたびに回す)
| ペルソナ | 見るもの |
|---|---|
| UXデザイナー | 情報設計・導線・一貫性 |
| **コピーライター/UXライター** | ボタン名・マイクロコピー・エラー文。**言葉と実態が合っているか** |
| CX担当 | 初回〜継続の体験曲線・感情 |
| QAエンジニア | 境界値・状態遷移・再現手順 |
| **デジタル不慣れ(60代の親)** | 説明なしで分かるか・専門語が無いか |

コピーライターとデジタル不慣れをコアに入れたのは、今回の「1問も答えていないのに"挑戦済み"」が
**言葉と実態の不一致**であり、コピーライター視点が最も鋭く突く類だったため。

### リリース前+6(App Store提出前・大型リリース前)
| ペルソナ | 見るもの |
|---|---|
| **App Store審査官** | ガイドライン(特に4.2「ネイティブらしさ」)。提出直前は最重要 |
| アクセシビリティ専門家 | コントラスト・文字拡大・VoiceOver・色覚 |
| UIデザイナー/ビジュアル | 余白・色・タイポ・階層 |
| プライバシー担当 | データ最小化・同意・ポリシー整合 |
| 片手操作/電車内のユーザー | 親指の届く範囲にボタンがあるか |
| 通りすがり(SNSから1回だけ) | 初見で何のアプリか分かるか |

### 随時14(大型追加時・伸び悩み時・手が空いたとき)
マーケター/グロース、ブランド担当、パフォーマンス担当、日本語表現/ローカライズ、
法務・コンプラ、データアナリスト、カスタマーサポート、
せっかち、やりこみ勢、疑り深い人、リピーター、子ども・若年層、
文字を大きくしている人、細い回線。

## 層5: 意地悪・モンキーテスト(`usual-test` Skill)

既にSkillに詳細がある。**隔離環境・実データを汚さない・390×664で見る**が大前提。

---

## いつ回すか

| タイミング | 層1 | 層2 | 層3 | 層4 | 層5 |
|---|---|---|---|---|---|
| コード変更のたび | ✅ | | | | |
| 機能追加・仕様変更 | ✅ | ✅ | | | |
| App Store提出前 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 実機FBを受けた後 | ✅ | ✅ | ✅ | | |

## バックログ(未整備)

- [ ] `npm test` の実装(層1)。**最優先**
- [ ] CI(GitHub Actions)でpush時に層1を自動実行する
- [ ] 層3のチェックを実機で1周する(今回①以外は未確認)
- [ ] 層4を1周する(前回いつ回したか記録が無い)
