# 次回リリースに向けたTODO

2026-08-23、平凡投票 v1.0 の申請を終えた時点での積み残し。

---

## A. fastlane deliver の導入(掲載情報の自動化)★最優先

**なぜ**: 今回、説明文・キーワード・スクショ・App Privacy・年齢制限を**すべて手で入力**した。
所要は1時間半ほど。次回のアップデートでも同じ画面を回ることになる。
`fastlane deliver` を使うと、**リポジトリのテキストと画像からコマンド1発で反映**できる。

**今回あえて手でやった理由**: 初回はApp Store Connect側の項目を把握しておく方が安全で、
何が必須かを知らないまま自動化すると、抜けに気づけない。**その把握は済んだ**ので、
次からは自動化してよい。

**やること**:
1. `gem install fastlane`(またはCodemagicのイメージに同梱のものを使う)
2. `fastlane deliver init` で既存の掲載情報をダウンロードし、`fastlane/metadata/` に落とす
   → **いま手で入れた内容がそのままファイルになる**ので、ゼロから書かなくてよい
3. `appstore/掲載情報_コピペ用.txt` と二重管理にしないこと。**metadata/ を一次情報源にする**
   か、txtから生成するかを決める(共通標準「一つの事実源」)
4. スクリーンショットは `fastlane/screenshots/ja/` に置く。
   `appstore/generate-screenshots.js` の出力先をそこに向ければ、**生成→アップロードが繋がる**
5. 認証は App Store Connect APIキー(`heibon-vote-asc` と同じもの)を使う。
   **`.p8` はリポジトリに置かない**
6. `codemagic.yaml` に `fastlane deliver --skip_binary_upload` のステップを足すかは、
   **審査提出まで自動化したいか**で決める(メタデータ更新だけ自動、提出は手動、が無難)

**注意**: くすっとゲームズと **`heibon-vote-asc` / `ios_signing` を共有**している。
Codemagic側でこれらの名前を変えると**くすっとのビルドも止まる**。

## B. アプリ本体へのFB(申請で聞かれたことから逆算)

App Store Connect の質問は「Appleがアプリの何を気にしているか」の一覧でもある。
今回聞かれて**答えに詰まった/実物を確認する必要があった**ものは、
そのままアプリ側の改善点になる。

| # | 申請での質問 | アプリへのFB | 効果 |
|---|---|---|---|
| 1 | 「ユーザ生成コンテンツはありますか」 | **提案フォームの投稿が他ユーザーに見えない**ことを、**アプリ内でも明示**する。いまは審査メモにしか書いていない。投稿者は「晒されるのでは」と不安になり得る | 中 |
| 2 | 「アルコール・タバコの言及」で**実際のお題を検索する必要があった** | **お題にコンテンツ属性のタグを持たせる**(alcohol/tobacco/gambling等)。次回以降、申告の根拠を検索でなく**データで**出せる。お題追加時に付けるだけ | 中 |
| 3 | 「健康またはウェルネス関連」で迷った | 「健康・カラダ」カテゴリの位置づけを決める。**生活習慣アンケートであって健康アドバイスではない**ことを、カテゴリ説明に一言入れると誤解が減る | 低 |
| 4 | 「データ削除の導線」 | いまは「localStorageを消す」しか手段がない。**アプリ内に「記録を消す」ボタン**があると親切(Appleの削除義務の対象外だが、あって困らない) | 中 |
| 5 | 年齢制限が **13+** になった | 4+ にしたいなら「お酒」「タバコ」の2問を非公開にする選択肢がある。**ただし実態に合った申告を優先すべき**なので、無理に下げない方がよい | 低 |
| 6 | プライバシーポリシーURLとデータ申告が**別入力**だった | アプリ側の話ではないが、**RELEASE-KITに1行足す**価値あり(「App Privacyは2箇所ある」) | — |
| 7 | スクショの**1枚目しか見られない**前提 | **結果画面の見栄え**が集客に直結する。「これまで◯人が挑戦」の人数が少ないうちは弱いので、母集団が増えたら撮り直す | 中 |

## C. 提出後にやること

- [ ] 審査結果のメールを待つ(初回は1〜3日)
- [ ] 承認されたら**手動でリリース**(自動公開にしていない)
- [ ] 公開後、**最初の実ユーザーの回答が入ることを確認**(共通標準5章「最初の本番実行を見届けるまでが変更作業」)
- [ ] `PLAN-リピーター施策.md` の第1段階(17問追加→未公開6カテゴリ公開)を判断
